#include "stm32f4xx_hal.h"
#include "usart.h"  // ?? USART ?????
#include "syscalls.h"

// ????????????? printf ??? USART3
int _write(int file, char *ptr, int len)
{
    // ??????????(???)
    HAL_UART_Transmit_IT(&huart3, (uint8_t*)ptr, len);  // ?? USART3 ???????????
    return len;  // ??????????
}